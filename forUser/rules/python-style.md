# Python スタイルルール（.claude/rules/python-style.md の日本語版）

対象パス: `src/**/*.py`, `tests/**/*.py`

- 全パブリック関数・クラス属性に型ヒントを付ける
- `Any` 型は原則禁止。使う場合はなぜ必要かをコメントで説明
- ファイルが 300 行を超えたらモジュール分割を検討
- 複数行 docstring は書かない。コメントは「なぜそうなのか」が自明でない場合のみ 1 行
- JAX 配列は非自明な shape をコメントで補足する (例: `# (n, dim)`)
